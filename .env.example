# Set to 0 to run tests/integration against GenLayer StudioNet.
SKIP_INTEGRATION=1

# The live suite needs no keys: it creates throwaway accounts and funds
# them from the StudioNet faucet. Optionally reuse a deployment:
STATELOCK_CONTRACT=

# The web app reads its own public configuration from app/.env.local
# (see app/.env.example).
